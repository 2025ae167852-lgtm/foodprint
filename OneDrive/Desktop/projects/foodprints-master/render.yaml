services:
  - type: web
    name: foodprints
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: foodprints-db
          property: connectionString
    healthCheckPath: /
    autoDeploy: true

databases:
  - name: foodprints-db
    plan: starter
    databaseName: foodprints
    user: foodprints_user

